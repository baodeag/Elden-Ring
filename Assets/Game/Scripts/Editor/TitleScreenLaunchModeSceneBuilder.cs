namespace baodeag.Editor
{
    // Scene-authored launch mode UI now lives directly in Main_Menu_01.unity.
    // This stub remains only so existing generated project files still compile
    // until Unity regenerates the editor csproj.
    internal static class TitleScreenLaunchModeSceneBuilder
    {
    }
}
